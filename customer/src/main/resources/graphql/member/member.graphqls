extend type Query {
    members: [Member]
}

type Member {
    id: ID
    nickname: String
    email: String
    isNotificationEnabled: Boolean
    profile: String
    oauthProvider: String
    isAdmin: Boolean
    isMondayStart: Boolean
}